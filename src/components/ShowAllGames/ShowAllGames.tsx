const ShowAllGames = () => {
    return (
        // <div className={`ShowAllGames ${isSelected() && "selected"} ${clicked && "clicked"}`} style={{"--click-duration": `${clickAnimationDurationInMs}ms`} as React.CSSProperties}>
        //     <div className="bubble" onClick={handleClick}>
        //         <div className={`icon ${title}`}>
        //             <img src={`${systemapps}/${title}.png`} alt={title} draggable="false" />
        //         </div>
        //         <Title title={title} visible={selectedIdValue == title} target="app" size="small"/>
        //     </div>
        // </div>
        <></>
    );
}

export default ShowAllGames;